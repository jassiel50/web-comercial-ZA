---
import Layout from '../layouts/Layout.astro';

const projects = [
  {
    badge: 'ERP · Inventario',
    title: 'Sistema de control de inventario y almacén',
    summary: 'Reemplazo de hojas de cálculo y procesos manuales por un panel centralizado con trazabilidad de stock, movimientos auditados y reportes al instante.',
    tags: ['Blazor WebAssembly', 'SQL Server', 'Radzen'],
    href: '/caso-inventario',
    imgSrc: '/images/erp-dashboard.jpg',
  },
  {
    badge: 'Portal · Roles',
    title: 'Portal de gestión para clientes y proveedores',
    summary: 'Un portal con roles y permisos donde cada cliente ve solo su información — pedidos, documentos y estados — sin depender de correos sueltos.',
    tags: ['Blazor WebAssembly', 'C#', 'SQL Server'],
    href: '/caso-portal',
    imgSrc: '/images/server-room.jpg',
  },
  {
    badge: 'Plataforma · Accesos',
    title: 'Plataforma administrativa con acceso seguro por roles',
    summary: 'Autenticación robusta y control granular de permisos para equipos internos de distintas áreas, con administración de accesos autogestionada.',
    tags: ['C#', '.NET', 'Radzen', 'SQL Server'],
    href: '/caso-plataforma',
    imgSrc: '/images/network-security.jpg',
  },
];

const stats = [
  { value: '3', label: 'Sistemas construidos', color: '#6ee7b7' },
  { value: '100%', label: 'A la medida', color: '#9184d9' },
  { value: 'Web + Móvil', label: 'Multiplataforma', color: '#f5b971' },
];
---
<Layout title="Proyectos — ZA Desarrollo Software" footerVariant="full">
  <main>
    <section style="background: radial-gradient(1000px 500px at 92% -20%, color-mix(in srgb, var(--color-accent-900) 55%, transparent), transparent 60%), var(--color-bg)">
      <div class="wrap" style="padding-block: 32px 24px">
        <span style="display: block; font-family: var(--font-mono); font-size: 13px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--color-accent); margin-bottom: 18px">Proyectos</span>
        <h1 style="font-family: var(--font-heading); font-weight: var(--font-heading-weight); font-size: clamp(28px,4.4vw,42px); margin: 0; max-width: 24ch">Sistemas hechos para reemplazar procesos manuales.</h1>
        <p style="font-size: 16px; line-height: 1.6; color: color-mix(in srgb, var(--color-text) 72%, transparent); max-width: 60ch; margin: 20px 0 0">Cada proyecto nace de un problema operativo concreto: información dispersa, procesos manuales o falta de control. Los siguientes son ejemplos representativos del tipo de sistemas que construyo — las capturas y datos mostrados no corresponden a clientes reales.</p>
        <div style="display: flex; flex-wrap: wrap; gap: 28px; margin-top: 28px">
          {stats.map((s) => (
            <div style="padding-left: 16px; border-left: 1px solid var(--color-divider)">
              <span style={`display: block; font-family: var(--font-heading); font-weight: var(--font-heading-weight); font-size: 20px; color: ${s.color}`}>{s.value}</span>
              <span style="display: block; font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.06em; text-transform: uppercase; color: color-mix(in srgb, var(--color-text) 55%, transparent); margin-top: 4px">{s.label}</span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section style="background: var(--color-surface)">
      <div class="wrap" style="padding-block: 40px 96px; display: flex; flex-direction: column; gap: 24px">
        {projects.map((p, i) => (
          <a href={p.href} data-reveal="1" class="card elev-sm hover-lift stack-mobile" style="display: grid; grid-template-columns: minmax(0,1fr) minmax(0,1.3fr); gap: 28px; align-items: center; text-decoration: none; color: inherit; background: var(--color-bg)">
            <div style="position: relative; width: 100%; aspect-ratio: 16/10; border-radius: var(--radius-sm); overflow: hidden">
              <img src={p.imgSrc} alt={p.title} class="img-cover" />
              <div class={i % 2 === 0 ? 'float-slow' : 'float-slow-delay'} style="position: absolute; top: 12px; left: 12px; display: inline-flex; align-items: center; gap: 7px; font-family: var(--font-mono); font-size: 10.5px; letter-spacing: 0.04em; color: color-mix(in srgb, var(--color-text) 88%, transparent); background: color-mix(in srgb, var(--color-bg) 68%, transparent); backdrop-filter: blur(10px); border: 1px solid color-mix(in srgb, var(--color-text) 12%, transparent); border-radius: 999px; padding: 6px 12px">
                <span style="width: 6px; height: 6px; border-radius: 50%; background: #6ee7b7; flex: none; animation: waPulse 2s ease-in-out infinite"></span>
                {p.badge}
              </div>
            </div>
            <div style="display: flex; flex-direction: column; gap: 12px">
              <h2 class="card-title" style="font-size: 22px">{p.title}</h2>
              <p class="card-body" style="opacity: 1; color: color-mix(in srgb, var(--color-text) 72%, transparent)">{p.summary}</p>
              <div style="display: flex; flex-wrap: wrap; gap: 6px">
                {p.tags.map((t) => (
                  <span class="tag tag-accent" style="font-family: var(--font-mono)">{t}</span>
                ))}
              </div>
              <span style="font-size: 14.5px; font-weight: 500; color: var(--color-accent); margin-top: 4px">Ver caso de estudio →</span>
            </div>
          </a>
        ))}
      </div>
    </section>
  </main>
</Layout>
